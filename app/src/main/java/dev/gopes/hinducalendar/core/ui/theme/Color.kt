package dev.gopes.hinducalendar.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light Mode ──────────────────────────────────────────────────────────────

val DeepSaffron = Color(0xFFE88A2D)         // Primary
val SacredMaroon = Color(0xFF7B1A3A)        // Secondary
val DivineGold = Color(0xFFC5A028)          // Tertiary
val WarmCream = Color(0xFFFFF8F0)           // Background
val SoftIvory = Color(0xFFFFFDF7)           // Surface
val SoftIvoryElevated = Color(0xFFFFFEFC)   // Surface elevated
val AuspiciousGreen = Color(0xFF2E7D32)     // Auspicious periods
val InauspiciousRed = Color(0xFFC62828)     // Inauspicious periods

// On-colors (light)
val WarmNearBlack = Color(0xFF261A14)       // On background
val WarmOnSurface = Color(0xFF33261F)       // On surface
val WarmOnSurfaceSecondary = Color(0xFF736659) // Secondary text
val OnDeepSaffron = Color(0xFFFFFFFF)       // On primary
val OnSacredMaroon = Color(0xFFFFFFFF)      // On secondary
val LightDivider = Color(0xFFD9CCC0)        // Dividers

// Containers (light)
val SaffronContainer = Color(0xFFFFF0E0)    // Primary container
val OnSaffronContainer = Color(0xFF4E2600)  // On primary container
val MaroonContainer = Color(0xFFF5E0E8)     // Secondary container
val GoldContainer = Color(0xFFFFF8E0)       // Tertiary container

// ── Dark Mode ───────────────────────────────────────────────────────────────

val WarmGold = Color(0xFFD4A046)            // Primary dark
val SoftRose = Color(0xFFC97B8B)            // Secondary dark
val DarkTertiary = Color(0xFFDEC059)        // Tertiary dark
val DeepSacred = Color(0xFF1A1118)          // Background dark
val DarkSurface = Color(0xFF2A2028)         // Surface dark
val DarkSurfaceElevated = Color(0xFF382D36) // Surface elevated dark
val DarkAuspicious = Color(0xFF4DB04F)      // Auspicious dark
val DarkInauspicious = Color(0xFFEF6666)    // Inauspicious dark

// On-colors (dark)
val DarkOnBackground = Color(0xFFEDE6DF)    // On background dark
val DarkOnSurface = Color(0xFFE0D9D2)       // On surface dark
val DarkOnSurfaceSecondary = Color(0xFF9E9590) // Secondary text dark
val OnWarmGold = Color(0xFF1A1108)          // On primary dark
val DarkDivider = Color(0xFF4D4048)         // Dividers dark

// Containers (dark)
val DarkSaffronContainer = Color(0xFF3D2A12) // Primary container dark
val DarkOnSaffronContainer = Color(0xFFFFE0B0) // On primary container dark
val DarkMaroonContainer = Color(0xFF3D1828)  // Secondary container dark
val DarkGoldContainer = Color(0xFF3D3412)    // Tertiary container dark

// ── Vibrant Light Mode (brighter, more saturated) ─────────────────────────

val VibrantSaffron = Color(0xFFF28019)          // (0.95, 0.50, 0.10)
val VibrantMaroon = Color(0xFF941F4D)           // (0.58, 0.12, 0.30)
val VibrantGold = Color(0xFFD9B31A)             // (0.85, 0.70, 0.10)
val VibrantAuspicious = Color(0xFF218C21)       // (0.13, 0.55, 0.13)
val VibrantInauspicious = Color(0xFFD91C1C)     // (0.85, 0.11, 0.11)

// ── Vibrant Dark Mode ─────────────────────────────────────────────────────

val VibrantDarkPrimary = Color(0xFFE6AD38)      // (0.90, 0.68, 0.22)
val VibrantDarkSecondary = Color(0xFFE08094)    // (0.88, 0.50, 0.58)
val VibrantDarkTertiary = Color(0xFFF2D14D)     // (0.95, 0.82, 0.30)
val VibrantDarkAuspicious = Color(0xFF40C740)   // (0.25, 0.78, 0.25)
val VibrantDarkInauspicious = Color(0xFFFF5959) // (1.0, 0.35, 0.35)

// ── Celestial Colors ────────────────────────────────────────────────────────

val SunriseColor = Color(0xFFE88A2D)            // (0.91, 0.54, 0.18) Saffron
val SunsetColor = Color(0xFFCC4033)             // (0.80, 0.25, 0.20) Warm Red
val MoonriseColor = Color(0xFF5C6BC0)           // (0.36, 0.42, 0.75) Indigo
val MoonsetColor = Color(0xFF7E57C2)            // (0.49, 0.34, 0.76) Purple

// ── Tradition Colors ────────────────────────────────────────────────────────

val TraditionVaishnav = Color(0xFFE88A2D)
val TraditionShaiv = Color(0xFF5C6BC0)
val TraditionShakta = Color(0xFFC62840)
val TraditionSmarta = Color(0xFF998033)
val TraditionISKCON = Color(0xFFE88A2D)
val TraditionSwaminarayan = Color(0xFFD96626)
val TraditionSikh = Color(0xFF3366B3)
val TraditionJain = Color(0xFFB0B0B0)
val TraditionGeneral = Color(0xFFC5A028)
